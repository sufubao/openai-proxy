# Prometheus 配置
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'openai-proxy'
    static_configs:
      - targets: ['openai-proxy:8000']
    metrics_path: '/metrics'
